# Task

Build a C++ command-line tool that monitors a list of URLs for uptime. The tool checks URLs on a configurable schedule, tracks consecutive failures, and prints notifications to stdout when a site transitions between UP and DOWN states.

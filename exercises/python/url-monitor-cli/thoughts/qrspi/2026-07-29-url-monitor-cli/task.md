# Task

Build a Python CLI tool that monitors a list of URLs for uptime. It checks each
URL on a schedule and prints notifications when a site transitions state — going
down (unreachable or unhealthy) or coming back up. The goal is a small,
self-contained monitoring utility a user can run from the terminal.
